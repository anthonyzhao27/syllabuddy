export function EventList() {
  return (
    <div className="w-full max-w-2xl">
      {/* TODO: Display parsed assignments with dates */}
      {/* TODO: Flag ambiguous dates for user confirmation */}
      <p className="text-sm text-zinc-400 text-center">No assignments parsed yet.</p>
    </div>
  );
}
